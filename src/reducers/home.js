import * as ActionTypes from '../actions/home'

const feature = (state = {
  exchange: undefined,
  base: undefined,
  help: undefined,
}, action) => {
  switch(action.type) {
    case ActionTypes.
  }
}